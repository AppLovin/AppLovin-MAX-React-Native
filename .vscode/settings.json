{
    "git.ignoreLimitWarning": true,

    /* To use Path Intellisense instead of the default autocompletion, the following configuration option must be added to your settings: */
    "typescript.suggest.paths": false,

    /* To ensure "Prettier - Code Formatter" is used over other extensions you may have installed, set it as the default formatter: */
    "editor.defaultFormatter": "esbenp.prettier-vscode",
    "cSpell.words": [
        "Lovin"
    ]
}